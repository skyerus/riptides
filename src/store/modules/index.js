const getDefaultState = () => {
  return {
  }
};

const state = getDefaultState();

const getters = {
};

const actions = {
};

const mutations = {
};

export default {
  state,
  getters,
  actions,
  mutations
}